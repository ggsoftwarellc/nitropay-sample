{
    "scripts": {
        "build": "./node_modules/.bin/gulp",
        "start": "./node_modules/.bin/http-server --port 4209",
        "push": "git push origin `git subtree split --prefix dist master`:gh-pages --force"
    },
    "dependencies": {
        "del": "^5.1.0",
        "gulp": "^4.0.2",
        "gulp-clean": "^0.4.0",
        "gulp-file-include": "^2.1.1",
        "http-server": "^0.12.1"
    }
}
