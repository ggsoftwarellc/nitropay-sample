{
    "scripts": {
        "build": "./node_modules/.bin/gulp",
        "start": "./node_modules/.bin/gulp watch",
        "pub": "git subtree split --prefix dist -b gh-pages && git push -f origin gh-pages:gh-pages && git branch -D gh-pages"
    },
    "dependencies": {
        "del": "^5.1.0",
        "gulp": "^4.0.2",
        "gulp-clean": "^0.4.0",
        "gulp-file-include": "^2.1.1",
        "gulp-token-replace": "^1.1.5",
        "gulp-watch": "^5.0.1",
        "gulp-webserver": "^0.9.1",
        "http-server": "^0.12.1"
    },
    "devDependencies": {
        "debug": "2.6.9",
        "lodash.template": "4.5.0",
        "open": "6.0.0"
    }
}
